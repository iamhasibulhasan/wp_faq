<?php
/**
 * Theme hooks.
 * @author  	 DeoThemes
 * @copyright  (c) Copyright by DeoThemes
 * @link       https://deothemes.com
 * @package 	 Arendelle
 * @since 		 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function arendelle_head_top() {
	do_action( 'arendelle_head_top' );
}

/**
 * Head bottom
 */
function arendelle_head_bottom() {
	do_action( 'arendelle_head_bottom' );
}

/**
 * Header before
 */
function arendelle_header_before() {
	do_action( 'arendelle_header_before' );
}

/**
 * Site Header
 */
function arendelle_header() {
	do_action( 'arendelle_header' );
}

/**
 * Header after
 */
function arendelle_header_after() {
	do_action( 'arendelle_header_after' );
}

/**
 * Masthead
 */
function arendelle_masthead() {
	do_action( 'arendelle_masthead' );
}

/**
 * Menu before
 */
function arendelle_menu_before() {
	do_action( 'arendelle_menu_before' );
}

/**
 * Menu after
 */
function arendelle_menu_after() {
	do_action( 'arendelle_menu_after' );
}

/**
 * Last menu item after
 */
function arendelle_last_menu_item_after() {
	do_action( 'arendelle_last_menu_item_after' );
}

/**
 * Body top
 */
function arendelle_body_top() {
	do_action( 'arendelle_body_top' );
}

/**
 * Body bottom
 */
function arendelle_body_bottom() {
	do_action( 'arendelle_body_bottom' );
}

/**
 * Primary content top
 */
function arendelle_primary_content_top() {
	do_action( 'arendelle_primary_content_top' );
}

/**
 * Primary content before
 */
function arendelle_primary_content_before() {
	do_action( 'arendelle_primary_content_before' );
}

/**
 * Primary content query
 */
function arendelle_primary_content_query() {
	do_action( 'arendelle_primary_content_query' );
}

/**
 * Primary content after
 */
function arendelle_primary_content_after() {
	do_action( 'arendelle_primary_content_after' );
}

/**
 * Primary content bottom
 */
function arendelle_primary_content_bottom() {
	do_action( 'arendelle_primary_content_bottom' );
}

/**
 * Comments before
 */
function arendelle_comments_before() {
	do_action( 'arendelle_comments_before' );
}


/**
 * Comments
 */
function arendelle_comments() {
	do_action( 'arendelle_comments' );
}

/**
 * Comments after
 */
function arendelle_comments_after() {
	do_action( 'arendelle_comments_after' );
}


/**
 * Entry section before
 */
function arendelle_entry_section_before() {
	do_action( 'arendelle_entry_section_before' );
}

/**
 * Entry header before
 */
function arendelle_entry_header_before() {
	do_action( 'arendelle_entry_header_before' );
}

/**
 * Entry header
 */
function arendelle_entry_header() {
	do_action( 'arendelle_entry_header' );
}

/**
 * Entry header after
 */
function arendelle_entry_header_after() {
	do_action( 'arendelle_entry_header_after' );
}

/**
 * Content before
 */
function arendelle_content_before() {
	do_action( 'arendelle_content_before' );
}

/**
 * Content top
 */
function arendelle_content_top() {
	do_action( 'arendelle_content_top' );
}

/**
 * Content bottom
 */
function arendelle_content_bottom() {
	do_action( 'arendelle_content_bottom' );
}

/**
 * Content after
 */
function arendelle_content_after() {
	do_action( 'arendelle_content_after' );
}


/**
 * Entry featured image
 */
function arendelle_entry_featured_image() {
	do_action( 'arendelle_entry_featured_image' );
}

/**
 * Entry content top
 */
function arendelle_entry_content_top() {
	do_action( 'arendelle_entry_content_top' );
}

/**
 * Entry content bottom
 */
function arendelle_entry_content_bottom() {
	do_action( 'arendelle_entry_content_bottom' );
}

/**
 * Page title before
 */
function arendelle_page_title_before() {
	do_action( 'arendelle_page_title_before' );
}

/**
 * Page title after
 */
function arendelle_page_title_after() {
	do_action( 'arendelle_page_title_after' );
}


/**
 * Sidebar before
 */
function arendelle_sidebar_before() {
	do_action( 'arendelle_sidebar_before' );
}

/**
 * Sidebar after
 */
function arendelle_sidebar_after() {
	do_action( 'arendelle_sidebar_after' );
}

/**
 * Footer before
 */
function arendelle_footer_before() {
	do_action( 'arendelle_footer_before' );
}

/**
 * Footer
 */
function arendelle_footer() {
	do_action( 'arendelle_footer' );
}

/**
 * Footer after
 */
function arendelle_footer_after() {
	do_action( 'arendelle_footer_after' );
}